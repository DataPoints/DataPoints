<h1>user List</h1>
<br />

<table class="table" style="color:white">
  <th class="col-md-5">Name</th>
  <th class="col-md-5">Email</th>
  <th class="col-md-2">Action</th>
  <% @users.each do |u| %>
      <tr>
        <td>
          <%= u.name %>
        </td>
        <td>
          <%= u.email %>
        </td>
        <td>
          <%= link_to 'Destroy',
                      url_for(:controller => 'admin/dashboard', :action => 'destroy', :id => u.id),
                      method: :delete,
                      data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
  <% end %>
</table>
<div>
  <%= paginate @users %>
</div>